---
// index.astro
import '../styles/global.css';
---

<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bahos & Burbano | Derecho | Empresa | Estado</title>
    
    <link href="https://fonts.googleapis.com/css2?family=B612+Mono&family=Libre+Baskerville:ital@0;1&family=Inter:wght@300;400&display=swap" rel="stylesheet">
    
    <style>
        /* DEFINICIÓN DIRECTA DE LA FUENTE CLASH DISPLAY */
        /* Usamos exactamente el archivo que tienes: ClashDisplay-Medium.woff2 */
        @font-face {
            font-family: 'ClashDisplay';
            src: url('/fonts/ClashDisplay-Medium.woff2') format('woff2');
            font-weight: 500; /* Medium */
            font-style: normal;
            font-display: swap;
        }
    </style>
</head>
<body>

    <nav class="nav-header">
        <div class="nav-links">
            <a href="/">Inicio</a>
            <a href="/areas-de-practica">Áreas de Práctica</a>
            <a href="/nosotros">Nosotros</a>
            <a href="/talento">Talento</a>
            <a href="/insights">Insights</a>
            <a href="/contactenos">Contáctenos</a>
        </div>
    </nav>

    <main class="hero-container">
        
        <h1 class="brand-identity">
            Bahos <span class="amp">&</span> Burbano
        </h1>
        
        <div class="descriptor">
            Derecho | Empresa | Estado
        </div>
        
        <div class="gold-divider"></div>
        
        <p class="mission-statement">
            Soluciones jurídicas de alta complejidad en la intersección de los sectores público y privado.
        </p>

        <a href="/nosotros" class="cta-link">Conozca la Firma</a>

    </main>

    <footer class="minimal-footer">
        © 2026 BAHOS <span class="amp-footer">&</span> BURBANO. TODOS LOS DERECHOS RESERVADOS.
    </footer>

</body>
</html>

<style>
    /* ESTILOS DE LA PORTADA */
    
    :root {
        --color-firma: #1F4037;
        --color-oro: #8A704D;
        /* Forzamos el uso de la fuente definida arriba */
        --font-clash: 'ClashDisplay', sans-serif; 
        --font-mono: 'B612 Mono', monospace;
    }

    body {
        margin: 0;
        background-color: #FDFDFB; /* Blanco editorial */
    }

    /* Navbar */
    .nav-header {
        padding: 2.5rem 0;
        display: flex; justify-content: center;
        background: rgba(253,253,251, 0.98);
        position: fixed; top: 0; width: 100%; z-index: 1000;
    }

    .nav-links { display: flex; gap: 2.5rem; }

    .nav-links a {
        text-decoration: none;
        color: var(--color-firma);
        font-family: var(--font-clash); /* Clash también en el menú si gustas, o Inter */
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 0.15em;
        opacity: 0.6;
        transition: opacity 0.3s ease;
    }
    .nav-links a:hover { opacity: 1; }

    /* Hero */
    .hero-container {
        display: flex; flex-direction: column; align-items: center;
        justify-content: center; min-height: 100vh;
        text-align: center; padding: 0 2rem;
    }

    .brand-identity {
        font-family: var(--font-clash); /* APLICANDO CLASH DISPLAY */
        font-size: clamp(3rem, 9vw, 5rem);
        font-weight: 500; /* Coincide con el peso Medium */
        color: var(--color-firma);
        margin: 0;
        line-height: 1.1;
    }

    .amp {
        font-family: var(--font-mono); /* B612 Mono */
        font-weight: 400;
        font-size: 0.8em;
        color: var(--color-firma);
    }

    .descriptor {
        font-family: var(--font-clash); /* APLICANDO CLASH DISPLAY */
        font-size: 1.25rem;
        margin-top: 1.5rem;
        letter-spacing: 0.2em;
        color: var(--color-firma);
        font-weight: 500;
    }

    .gold-divider {
        width: 60px; height: 1px;
        background: var(--color-oro);
        margin: 4rem 0;
        opacity: 0.5;
    }

    .mission-statement {
        font-family: 'Libre Baskerville', serif;
        font-style: italic;
        font-size: 1.4rem;
        line-height: 1.8;
        max-width: 750px;
        color: var(--color-firma);
        margin-bottom: 4rem;
    }

    .cta-link {
        font-family: 'Inter', sans-serif;
        text-decoration: none;
        color: var(--color-firma);
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 0.2em;
        border-bottom: 1px solid var(--color-oro);
        padding-bottom: 5px;
    }

    .minimal-footer {
        text-align: center; padding: 4rem 0;
        font-family: 'Inter', sans-serif;
        font-size: 0.7rem; letter-spacing: 0.15em;
        opacity: 0.5;
        position: absolute; bottom: 0; width: 100%;
        color: var(--color-firma);
    }
    
    .amp-footer { font-family: var(--font-mono); }
</style>
