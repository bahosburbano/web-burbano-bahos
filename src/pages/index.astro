---
// src/pages/index.astro
import '../styles/global.css';
---

<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Bahos & Burbano | Arquitectura Jurídica</title>
    <link href="https://fonts.googleapis.com/css2?family=B612+Mono&family=Libre+Baskerville:ital@0;1&family=Inter:wght@300;400&display=swap" rel="stylesheet">
</head>
<body>

    <nav class="nav-header">
        <div class="nav-container">
            <div class="nav-links">
                <a href="/" class="active">Inicio</a>
                <a href="/la-firma">La Firma</a> <a href="/areas-de-practica">Práctica</a>
                <a href="/talento">Talento</a>
            </div>
            <a href="/contactenos" class="nav-cta">Consulta Estratégica</a>
        </div>
    </nav>

    <main>
        
        <section class="hero-section">
            <div class="hero-content">
                <span class="hero-descriptor">DERECHO <span class="amp">&</span> EMPRESA <span class="amp">&</span> ESTADO</span>
                <h1 class="hero-title">Arquitectura jurídica<br>de alto nivel.</h1>
                <p class="hero-subtitle">Incidencia estructural y blindaje estratégico<br>en entornos de alta complejidad.</p>
                
                <div class="hero-actions">
                    <a href="/contactenos" class="btn-primary">Iniciar Evaluación Técnica</a>
                    <a href="/areas-de-practica" class="btn-secondary">Explorar Áreas de Práctica</a>
                </div>
            </div>
        </section>

        <section class="differential-section">
            <div class="differential-content">
                <span class="section-label">Criterio de Autor</span>
                <blockquote class="differential-quote">
                    "Operamos bajo un modelo de involucramiento directo. No delegamos el núcleo intelectual de su asunto; lo resolvemos mediante una arquitectura normativa diseñada para resistir el escrutinio institucional."
                </blockquote>
            </div>
        </section>

        <section class="pillars-section">
            <div class="pillar-item">
                <h3 class="pillar-heading">Derecho Público <span class="amp">&</span><br>Constitucional</h3>
                <span class="pillar-arrow">→</span>
            </div>
            <div class="pillar-item">
                <h3 class="pillar-heading">Derecho Corporativo <span class="amp">&</span><br>Compliance</h3>
                <span class="pillar-arrow">→</span>
            </div>
            <div class="pillar-item">
                <h3 class="pillar-heading">Derecho Médico <span class="amp">&</span><br>Sanitario</h3>
                <span class="pillar-arrow">→</span>
            </div>
            <div class="pillar-item">
                <h3 class="pillar-heading">Movilidad Internacional <span class="amp">&</span><br>Migratorio</h3>
                <span class="pillar-arrow">→</span>
            </div>
        </section>

    </main>

    <footer class="main-footer">
        <div class="footer-content">
            <div class="footer-left">
                <span class="footer-logo">B <span class="amp">&</span> B</span>
                <span class="footer-descriptor">Derecho | Empresa | Estado</span>
            </div>
            <div class="footer-center">
                <a href="#">Privacidad</a>
                <a href="#">Términos</a>
            </div>
            <div class="footer-right">
                <p>Bogotá | Cali | Popayán | Ubicuidad Estratégica</p>
                <p class="copyright">© 2026 Bahos <span class="amp">&</span> Burbano.</p>
            </div>
        </div>
    </footer>

</body>
</html>

<style>
    /* ESTILOS INTERNOS (Optimizados) */
    .amp { font-family: var(--font-mono); font-style: normal; }
    
    .nav-header { padding: 2rem 3rem; background: rgba(253,253,251,0.98); position: fixed; top: 0; width: 100%; z-index: 1000; border-bottom: 1px solid rgba(31,64,55,0.05); box-sizing: border-box; }
    .nav-container { display: flex; justify-content: space-between; align-items: center; max-width: 1400px; margin: 0 auto; }
    .nav-links { display: flex; gap: 3rem; }
    .nav-links a { text-decoration: none; color: var(--color-firma); font-family: var(--font-descriptor); font-size: 0.8rem; text-transform: uppercase; letter-spacing: 0.2em; opacity: 0.6; transition: opacity 0.3s; }
    .nav-links a:hover, .nav-links a.active { opacity: 1; border-bottom: 1px solid var(--color-oro); }
    .nav-cta { text-decoration: none; color: #FDFDFB; background-color: var(--color-oro); font-family: var(--font-descriptor); font-size: 0.75rem; text-transform: uppercase; letter-spacing: 0.15em; padding: 0.8rem 1.5rem; transition: background 0.3s; }
    .nav-cta:hover { background-color: var(--color-firma); }

    .hero-section { height: 90vh; display: flex; align-items: center; justify-content: center; text-align: center; padding-top: 4rem; }
    .hero-content { max-width: 800px; padding: 0 2rem; }
    .hero-descriptor { display: block; font-family: var(--font-descriptor); font-size: 0.75rem; letter-spacing: 0.3em; margin-bottom: 2rem; opacity: 0.8; }
    .hero-title { font-family: var(--font-apellidos); font-size: 4.5rem; line-height: 1.1; margin-bottom: 1.5rem; letter-spacing: -0.02em; }
    .hero-subtitle { font-family: var(--font-body); font-weight: 300; font-size: 1.2rem; line-height: 1.6; opacity: 0.8; margin-bottom: 3rem; }
    .hero-actions { display: flex; justify-content: center; gap: 2rem; }
    
    .btn-primary { text-decoration: none; background-color: var(--color-firma); color: #FDFDFB; padding: 1rem 2.5rem; font-family: var(--font-descriptor); text-transform: uppercase; letter-spacing: 0.15em; font-size: 0.75rem; border: 1px solid var(--color-firma); transition: all 0.3s; }
    .btn-primary:hover { background-color: transparent; color: var(--color-firma); }
    .btn-secondary { text-decoration: none; background-color: transparent; color: var(--color-firma); padding: 1rem 2.5rem; font-family: var(--font-descriptor); text-transform: uppercase; letter-spacing: 0.15em; font-size: 0.75rem; border: 1px solid rgba(31,64,55,0.3); transition: all 0.3s; }
    .btn-secondary:hover { border-color: var(--color-firma); }

    .differential-section { padding: 8rem 2rem; background-color: #FDFDFB; text-align: center; border-top: 1px solid rgba(31,64,55,0.05); }
    .differential-content { max-width: 800px; margin: 0 auto; }
    .section-label { font-family: var(--font-descriptor); font-size: 0.7rem; text-transform: uppercase; letter-spacing: 0.2em; color: var(--color-oro); margin-bottom: 2rem; display: block; }
    .differential-quote { font-family: var(--font-serif); font-size: 1.8rem; font-style: italic; line-height: 1.5; margin: 0; color: var(--color-firma); }

    .pillars-section { display: grid; grid-template-columns: repeat(4, 1fr); border-top: 1px solid rgba(31,64,55,0.1); border-bottom: 1px solid rgba(31,64,55,0.1); }
    .pillar-item { padding: 4rem 2rem; border-right: 1px solid rgba(31,64,55,0.05); transition: background 0.3s; display: flex; flex-direction: column; justify-content: space-between; height: 250px; cursor: default; }
    .pillar-item:last-child { border-right: none; }
    .pillar-item:hover { background: rgba(31,64,55,0.02); }
    .pillar-heading { font-family: var(--font-apellidos); font-size: 1.4rem; line-height: 1.3; margin: 0; }
    .pillar-arrow { font-family: var(--font-mono); font-size: 1.2rem; color: var(--color-oro); text-align: right; opacity: 0; transition: opacity 0.3s; transform: translateX(-10px); }
    .pillar-item:hover .pillar-arrow { opacity: 1; transform: translateX(0); }

    .main-footer { background-color: #FDFDFB; padding: 4rem 3rem; font-size: 0.75rem; letter-spacing: 0.05em; color: var(--color-firma); }
    .footer-content { max-width: 1400px; margin: 0 auto; display: flex; justify-content: space-between; align-items: flex-end; }
    .footer-left { display: flex; flex-direction: column; gap: 0.5rem; }
    .footer-logo { font-family: var(--font-apellidos); font-size: 1.2rem; }
    .footer-descriptor { font-family: var(--font-descriptor); font-size: 0.65rem; text-transform: uppercase; letter-spacing: 0.15em; opacity: 0.7; }
    .footer-center a { text-decoration: none; color: inherit; margin: 0 1rem; opacity: 0.5; transition: opacity 0.3s; }
    .footer-center a:hover { opacity: 1; }
    .footer-right { text-align: right; opacity: 0.6; }
    .copyright { font-family: var(--font-mono); font-size: 0.65rem; margin-top: 0.5rem; }

    @media (max-width: 1024px) { .pillars-section { grid-template-columns: repeat(2, 1fr); } .pillar-item:nth-child(2) { border-right: none; } .pillar-item { border-bottom: 1px solid rgba(31,64,55,0.05); } }
    @media (max-width: 768px) { .hero-title { font-size: 3rem; } .hero-actions { flex-direction: column; gap: 1rem; } .pillars-section { grid-template-columns: 1fr; } .pillar-item { border-right: none; height: auto; padding: 3rem 2rem; } .nav-links { display: none; } .footer-content { flex-direction: column; align-items: center; gap: 2rem; text-align: center; } .footer-right { text-align: center; } }
</style>
