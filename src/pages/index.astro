---
/* index.astro - Versión Optimizada para Móvil y PC */
---
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <title>Bahos & Burbano | Derecho • Empresa • Estado</title>
    <meta name="description" content="Bahos & Burbano: Firma legal. Transformamos el conocimiento jurídico en soluciones integrales." />
    <link href="https://fonts.googleapis.com/css2?family=B612+Mono&display=swap" rel="stylesheet">
</head>
<body>
    <div class="bg-verde fade-general" role="main">
        <div class="container">
            <h1 class="brand-name">
                Bahos <span class="ampersand" aria-label="y">&</span> Burbano
            </h1>
            
            <p class="subtitle">Derecho | Empresa | Estado</p>
            
            <div class="divider" aria-hidden="true"></div>
            
            <h2 class="status">Sitio web en construcción</h2>
            
            <p class="message">
                Transformamos el conocimiento jurídico en soluciones para usted.<br><br>
                Actualmente, estamos perfeccionando nuestra plataforma digital para brindarle una experiencia de asesoría integral y de alto nivel.<br><br>
                Gracias por su paciencia.
            </p>

            <div class="cta-wrapper">
                <a href="mailto:contacto@bahosburbano.com" class="contact-btn" aria-label="Contactar a la firma">
                    Contactar por Correo
                </a>
            </div>

            <footer class="legal-footer">
                <p>© 2025 Bahos & Burbano. Todos los derechos reservados.</p>
            </footer>
        </div>
    </div>
</body>
</html>

<style is:global>
  @font-face {
    font-family: 'Clash Display Semibold';
    src: url('/ClashDisplay-Semibold.woff') format('woff');
    font-weight: 600; font-style: normal; font-display: swap;
  }

  @font-face {
    font-family: 'Clash Display Light';
    src: url('/ClashDisplay-Light.woff') format('woff');
    font-weight: 300; font-style: normal; font-display: swap;
  }

  :root {
    --color-principal: #16302A;
    --color-dorado: #d4af37;
    --fuente-titulo: 'Clash Display Semibold', sans-serif;
    --fuente-texto: 'Clash Display Light', sans-serif;
    --fuente-mono: 'B612 Mono', monospace;
  }

  html, body {
    margin: 0; padding: 0;
    background-color: var(--color-principal);
    /* Cambiamos height: 100% por min-height para permitir scroll en móviles */
    min-height: 100%;
    color: white;
  }

  .bg-verde {
    background-color: var(--color-principal);
    min-height: 100vh;
    width: 100%;
    display: flex;
    justify-content: center;
    align-items: center;
    text-align: center;
    position: relative;
    /* Permitimos que el contenido fluya si es más largo que la pantalla */
    padding: 40px 0;
  }

  .fade-general {
    animation: simpleFade 1.5s ease-in forwards;
  }

  @keyframes simpleFade {
    from { opacity: 0.85; }
    to { opacity: 1; }
  }

  .container {
    padding: 20px;
    width: 90%;
    max-width: 850px;
    z-index: 2;
  }

  .brand-name {
    font-family: var(--fuente-titulo);
    font-size: clamp(2rem, 8vw, 4.2rem); /* Tamaño fluido: se encoge en móvil */
    font-weight: 600;
    margin: 0;
    display: flex;
    flex-wrap: wrap; /* Permite que el nombre baje de renglón si es necesario */
    align-items: center;
    justify-content: center;
    gap: 0.1em;
  }

  .ampersand {
    font-family: var(--fuente-mono);
    font-size: 0.8em;
  }

  .subtitle {
    font-family: var(--fuente-texto);
    font-size: clamp(0.8rem, 3vw, 1.1rem);
    letter-spacing: 0.3em;
    margin-top: 1.5rem;
    text-transform: uppercase;
  }

  .divider {
    height: 1px;
    background: linear-gradient(to right, transparent, var(--color-dorado), transparent);
    margin: 2rem auto;
    width: 60%;
    max-width: 300px;
    opacity: 0.5;
  }

  .status {
    font-family: var(--fuente-titulo);
    font-size: clamp(1.1rem, 4vw, 1.4rem);
    margin-bottom: 1.5rem;
  }

  .message {
    font-family: var(--fuente-texto);
    font-size: clamp(0.95rem, 3.5vw, 1.2rem);
    line-height: 1.6;
    margin-bottom: 2.5rem;
    color: #cbd5e1;
  }

  .contact-btn {
    font-family: var(--fuente-titulo);
    display: inline-block;
    padding: 14px 35px;
    border: 1px solid var(--color-dorado);
    color: var(--color-dorado);
    text-decoration: none;
    font-size: 0.75rem;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    transition: all 0.3s ease;
  }

  .contact-btn:hover {
    background-color: var(--color-dorado);
    color: var(--color-principal);
  }

  .legal-footer {
    margin-top: 4rem;
    font-family: var(--fuente-texto);
    font-size: 0.7rem;
    color: #4a635d;
    padding-bottom: 20px;
  }

  /* AJUSTES ESPECÍFICOS PARA MÓVIL */
  @media (max-width: 600px) {
    .bg-verde {
      align-items: flex-start; /* Empieza desde arriba para que no se corte */
      padding-top: 60px;
    }
    .brand-name {
      flex-direction: column; /* Apila el nombre en móvil para mejor legibilidad */
      gap: 0;
    }
    .ampersand {
      margin: 5px 0;
    }
    .divider {
      width: 80%;
    }
    .legal-footer {
      position: static; /* Evita que el footer flote sobre el texto */
    }
  }
</style>
